(ns main)

(defn -main
  []
  (println "Hello, RBS!"))
; So Clojure is a variant of Lisp, and it's a functional programming language.
; LISP stands for List Processing, and it's the second oldest high-level programming language after Fortran.
; Basica idea we are processing lists of data.
; This data could be all sorts of things, but it's usually code.
; so code and data are interchangeable in LISP.

(-main)